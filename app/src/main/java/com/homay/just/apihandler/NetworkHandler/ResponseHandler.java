package com.homay.just.apihandler.NetworkHandler;

import com.google.gson.JsonArray;

public class ResponseHandler {
public ResponseHandler(){

}

    public ResponseHandler(JsonArray jsonArray) {
        if (jsonArray.size() == 1) {

        }

    }
}
